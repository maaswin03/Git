let fun = 20;
let run = 10;

let api_key = "#123"

function addNumber(a, b) {
  const answer = a + b;
}

function subNumber(a, b) {
  const answer = a - b;
}

function mulNumber(a, b) {
  const answer = a * b;
}

function divNumber(a, b) {
  const answer = a / b;
}

subNumber(fun, run);
addNumber(fun, run);
mulNumber(fun, run);
divNumber(fun, run);
